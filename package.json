{
  "name": "root",
  "private": true,
  "scripts": {
    "aws:ca": "aws codeartifact login --tool npm --domain simlex --domain-owner 523360775273 --repository npm-repo",
    "build:amplify": "lerna run build --scope=@milkylane/function --stream",
    "build:packages": "lerna run build --ignore=@milkylane/function --stream",
    "build": "lerna run build --stream",
    "bootstrap": "npm run aws:ca && lerna bootstrap",
    "publish": "npm run aws:ca && lerna publish"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
