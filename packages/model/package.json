{
  "name": "@milkylane/model",
  "version": "0.0.1",
  "main": "lib/index.js",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "npm run clean && tsc -b",
    "clean": "rimraf lib tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.3.5"
  }
}
