{
  "name": "@milkylane/model",
  "version": "0.0.0",
  "main": "lib/index.js",
  "files": [
    "lib",
    "package.json"
  ],
  "scripts": {
    "build": "npm run clean && tsc -b",
    "clean": "rimraf lib tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.3.5"
  }
}
